---
title: Set up the blog
date: 2026-08-08
description: First entry — Astro, two content collections, math, and dual-theme code.
tags: [meta, astro]
---

First worklog entry. The whole site is Astro with two content collections —
`worklogs` (this) and `posts` — rendered to fully static HTML with **zero client
JavaScript**.

A quick smoke test that the Markdown pipeline works end to end:

- Inline math renders with KaTeX — the gradient is
  $\nabla f = \left[\partial_x f,\ \partial_y f\right]$.
- Code is highlighted at build time with Shiki (github-light / github-dark):

```ts
export function slugify(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

Next up: write a real post.
